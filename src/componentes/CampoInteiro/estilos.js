import { StyleSheet } from "react-native";

import * as EstilosGlobais from "../../../estilos";

export const campo = StyleSheet.create({
    height: 25,
    borderWidth: 1,
    borderColor: EstilosGlobais.cores.escuro,
    textAlign: 'center',
});
